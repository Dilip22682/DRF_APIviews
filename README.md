# Django REST Framework API (APIView)

This project is a backend REST API developed using **Django REST Framework (DRF)** with **APIView**.  
It demonstrates how to build scalable and secure RESTful APIs using Django’s powerful ecosystem.

## 🚀 Features
- Built using DRF `APIView`
- CRUD operations (Create, Read, Update, Delete)
- JSON-based API responses
- Serializer-based data validation
- Error handling and status codes
- Modular and clean project structure

## 🛠 Tech Stack
- Python
- Django
- Django REST Framework
- SQLite / PostgreSQL

## 📌 API Endpoints
- GET /api/ – Retrieve data
- POST /api/ – Create new record
- PUT /api/<id>/ – Update record
- DELETE /api/<id>/ – Delete record
 
